rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Roommates expense tracker rules - read & write access for authenticated app
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
